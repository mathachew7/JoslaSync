# Gitignore for Multi-Tenant Invoicing System
docs.docx
# Node.js and npm/yarn related files
# These typically reside at the root if it's a monorepo,
# but also within the 'frontend' directory for a React app.
node_modules/
.env
.env.local
.env.development.local
.env.test.local
.env.production.local
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-store/
.npm/

# Frontend specific (React)
# Assuming 'frontend' is your React app's root directory
frontend/node_modules/
frontend/build/          # Production build output
frontend/dist/           # Another common build output directory
frontend/coverage/       # Jest/testing coverage reports
frontend/.env            # Local environment variables
frontend/.env.*.local    # Specific local environment variables
frontend/reportWebVitals.js # If generated by Create React App and not modified

# IDEs and OS specific files
.idea/                   # IntelliJ IDEA files
.vscode/                 # VS Code settings
*.iml                    # IntelliJ IDEA module files
.DS_Store                # macOS specific files
Thumbs.db                # Windows thumbnail cache files

# Logs and temporary files
logs/
*.log
*.tmp
*~
